# zenn-content

Zenn (https://zenn.dev/) で公開する記事と書籍を管理するリポジトリ。

## 日本語ライティングのチェック

`articles/**/*.md` および `books/**/*.md` を **新規作成または大幅編集** した際は、ファイル保存・コミット前に必ず `humanizer-ja` スキルで AI 臭を診断すること。

### 実行ルール

- **タイミング**：ドラフトが「これで公開できそう」と感じた直後
- **出力**：humanizer-ja は **指摘リストのみ** を出す（自動リライトしない）
- **修正**：指摘を受けて、ユーザーが手で直す。Claude が直すのは明示依頼があったときのみ
- **スキップ条件**：
  - frontmatter のみの変更
  - typo 修正、リンク差し替え
  - コードブロック・図表のみの追加
  - 既に humanize 済みでユーザーが「再診断不要」と明言している場合

### 既知の頻発パターン（ユーザー固有）

過去の診断で繰り返し検出された日本語 AI 臭：

- 「浮かび上がらせる / 浮き彫りにする」の連発
- 「〜ではなく、〜」否定並列構文の多用
- 接続詞「そのため」「一方で」の過剰使用
- 太字 + 「本質は」「重要なのは」のような誘導
- ですます調の語尾固定（緩急なし）
